import 'package:flutter/material.dart';

class MediceScreen extends StatelessWidget {
  const MediceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Medice')));
  }
}
